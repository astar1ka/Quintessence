class Image {
    constructor(x,y, shadow){
        this.shadow = shadow;
        this.x = x;
        this.y = y;
    }

    on(event, callback){
        switch (event) {
            case 'click': {
                break;
            }
        }
    }
}